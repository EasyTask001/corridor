# Mobile physical-device QA

Copy this file to `mobile-device-qa-<yyyy-mm-dd>.md`, delete this line, and
fill in every field. See `docs/operations/mobile-device-qa.md` for the
scenario table and the evidence JSON shape `verify-device-qa.mjs` expects.

## Summary

- **Date:**
- **App build / commit:**
- **iPhone model, OS version:**
- **Android model, OS version:**
- **Verified by:**
- **Outcome:** pass / fail

## Scenario results

Copy the 14-row table from `docs/operations/mobile-device-qa.md`, replacing
"Not run" with `passed` / `failed` and a one-line note for any failure.

| Scenario | iPhone | Android | Notes |
| --- | --- | --- | --- |
| | | | |

## Maestro golden path

- **Status:** passed / failed
- **Run link or log location:**

## Evidence file

- **Hosted at:** (the URL `CORRIDOR_MOBILE_QA_EVIDENCE_URL` points to — never
  paste the JSON itself here)
- **`recordedAt`:**
- **`commit`:**

## Follow-up (if failed)

- [ ] Scenario(s) that failed, and whether the fix is app code or test flake
- [ ] Re-run scheduled for:
