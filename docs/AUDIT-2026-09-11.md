# Corridor Audit — Issue List

Generated: 2026-09-11
Method: combined `app-audit-plan` + `audit-to-plan` + `code-review` + `security-review`, executed as 8 parallel
read-only audit passes (DB/migrations, API authorization, web app auth/server, domain/integrations, AI/PDF,
auth/ui/config, mobile, recent-changes code review). All CRITICAL and HIGH findings were independently
re-verified against the source after the agents reported them.

This was an audit only when generated — no fixes were applied at that time. Every non-`OK` item below
carries a `file:line` pointer and a quote of the decisive code where relevant.

**All 41 findings resolved on 2026-09-11 — see the Resolution line under each issue below for the fixing
commit(s) and any deliberate scoping.**

**Total: 41 findings — 1 CRITICAL · 5 HIGH · 20 MEDIUM · 15 LOW**

---

## CRITICAL

### ISSUE-001 — Cross-tenant data leak via `integrations.jobs.runNow`

**Verified.** `packages/api/src/router/integrations.ts:304`

Gated only by the caller's own-org `integrations.manage` permission, but calls `processDueJobs` →
`claim_jobs()` (`supabase/migrations/0033_background_job_leases.sql:14`), a `SECURITY DEFINER` function
revoked from `authenticated`/`anon` and meant only for the service-role worker. It claims and runs due jobs
**queue-wide across every organization** (no `organization_id` filter — `p_org_cap` only throttles per-org
concurrency within a claimed batch) and returns the full result, including other tenants' job outputs
(customs decisions, document extraction, notifications), straight to the caller. Any org admin with
`integrations.manage` can read other tenants' background-job results.

**Resolution:** Fixed in `865732f` — `runNow` now passes the caller's `organization_id` into `claim_jobs` (migration 0041's 5th parameter), scoping claimed jobs to that org, and returns counts only, never per-job `results`.

---

## HIGH

### ISSUE-002 — Drizzle schema is stale against migration 0031's tenant-safe composite FKs, across nearly every core table

**Verified.** `packages/db/src/schema/movements.ts:79` (+ integrations.ts, inbond.ts, documents.ts, alerts.ts, registry.ts)

Migration 0031 dropped ~30 single-column FKs (e.g. `movements_truck_id_fkey`) and replaced them with
composite `(col, organization_id) references parent(id, organization_id)` FKs plus ~50 new indexes/unique
constraints. None of this exists in the Drizzle schema — `movements.ts:79` still declares
`truckId: uuid(...).references(() => trucks.id, ...)`, the exact relationship the DB no longer has. Should
have been caught by `pnpm --filter @corridor/db verify:mirror`.

**Resolution:** Fixed in `246639b` (follow-up correctness fix in `170fe88`) — the Drizzle schema was brought current with migrations 0031/0034, and `verify:mirror` now checks FKs by matching table+columns (not constraint name) and flags unmirrored indexes.

### ISSUE-003 — Customs gateway fixture polling can never advance past its first stage

**Verified.** `packages/integrations/src/customs/gateway/client.ts:94` (`filings` map), vs
`packages/api/src/services/customs.ts:120-157` (`customsClientFor` builds a fresh client — and fresh
`filings` map — on every call)

Every poll resets to `polls: 0` → 1 → always renders `stages[0]`. A movement filed under gateway mode
without real CBP/CBSA credentials never reaches accepted/released/held/rejected. Contradicts the file's own
doc comment ("a poll sequence unfolds like a real crossing").

**Resolution:** Fixed in `aa290d6` (refined in `374752e`) — the gateway customs client's fixture state is now tenant-scoped and held in a bounded LRU+TTL map instead of a fresh per-call `filings` map, so polls advance through the stage sequence correctly.

### ISSUE-004 — In-bond mock/fixture state shared across all tenants in memory

**Verified.** `packages/integrations/src/customs/mock.ts:64` (`mockBonds`),
`packages/integrations/src/customs/gateway/client.ts:90` (`fixtureBonds`)

Both are module-level (process-wide) maps keyed only by `bondNumber`, read/written for every org using mock
or credential-less gateway mode. Two tenants picking the same demo bond number (plausible with synthetic
data) read/overwrite each other's in-bond status — a cross-tenant leak of application state, in memory
rather than a table, that runs against the repo's tenant-isolation principle. Also never evicted (unbounded
growth).

**Resolution:** Fixed in `aa290d6` (refined in `374752e`) — `mockBonds`/`fixtureBonds` were replaced with tenant-scoped, bounded (LRU+TTL) fixture state, closing the cross-tenant in-bond leak and the unbounded growth.

### ISSUE-005 — `customs.poll_status` makes an external HTTP call while holding an open DB transaction

**Verified.** `packages/api/src/services/jobs.ts:107` (still in `jobHandlers`, not `detachedJobHandlers`) →
`packages/api/src/services/customs.ts:652` (`await client.fetchStatus(ref)`)

The sibling `customs.decide` handler was already fixed for this (moved out of the transaction);
`customs.poll_status` — which re-enqueues itself every `POLL_INTERVAL_MS` until terminal or 48h — was
missed. A slow/hanging gateway pins a pooled DB connection for the call's duration on every poll.

**Resolution:** Fixed in `8cdd8cf` — `customs.poll_status` moved from `jobHandlers` to `detachedJobHandlers`, so the `fetchStatus` network call no longer runs inside an open DB transaction.

### ISSUE-006 — `DataTable` row click has no keyboard equivalent (WCAG 2.1.1 failure)

**Verified.** `packages/ui/src/components/data-table.tsx:216-229`, `packages/ui/src/components/table.tsx:28-32`

`onRowClick` is wired only to `onClick` on a bare `<tr>` — no `role`, `tabIndex`, or `onKeyDown`. A
keyboard-only user cannot reach or activate a row action at all. 3 consumers outside `packages/ui` use
`onRowClick`.

**Resolution:** Fixed in `1dcdf3a` — `DataTable` rows gain `tabIndex`, `onKeyDown`, and `aria-label` whenever `onRowClick` is set, making row activation keyboard-operable.

---

## MEDIUM

### API / Security

- **ISSUE-007** — CSV export vulnerable to Excel/Sheets formula injection: `csvField()` doesn't neutralize
  leading `=`/`+`/`-`/`@` — `packages/api/src/services/reporting-export.ts:18-22`, reachable via
  `party.ts:227-270`, `reporting.ts:147-195`.
  **Resolution:** Fixed in `01550ed` — `csvField()` now prefixes values starting with `=`/`+`/`-`/`@` with a leading `'` before RFC 4180 quoting, neutralizing formula injection.
- **ISSUE-008** — `reporting.crossings/export/run` require `report.read` AND `movement.read` (should likely
  be OR, like `pdf.ts`'s equivalent) — `packages/api/src/router/reporting.ts:137,147,202`. A
  "reporting-only" custom role with just `report.read` is silently locked out.
  **Resolution:** Fixed in `5b7315e` — `reporting.crossings/export/run` switched to `anyPermissionProcedure`, so `report.read` OR `movement.read` now suffices.
- **ISSUE-009** — Rate limiting fails open uniformly on Redis outage, including billing/invite/device-
  registration endpoints, with only a `console.error` — `packages/api/src/infra/ratelimit.ts:198-201,239-242`.
  **Resolution:** Fixed in `963cdca` — the rate limiter now falls back to the in-process sliding window (`getMemoryKv()`) on a store error instead of failing open. Also reflected in `docs/security-review.md` (F2).

### DB / Schema

- **ISSUE-010** — `jsonb` used for fixed-shape, always-read-by-name address fields across 4 tables
  (`partners.address`, `shipments.delivery_address`, `organizations.billing_address`, `drivers.us_address`),
  violating the "jsonb is for free-form metadata only" rule.
  **Resolution:** Fixed in `2c2108f`/`33c7f3e` — the four jsonb address columns were replaced with flat text columns (migration 0042); the API-facing `Address` shape is unchanged via `addressToColumns`/`nestAddress` helpers.
- **ISSUE-011** — SECURITY DEFINER `search_path` hardening was regressed by migration 0036
  (`set search_path = public`, unqualified refs) then re-fixed by 0038 — net-safe on a fully migrated DB,
  but shows the qualification discipline wasn't durable; worth a lint/CI guard.
  **Resolution:** Fixed in `0cc1f29` — a new standalone `packages/db/scripts/lint-migrations.ts`, run via `pnpm --filter @corridor/db lint`, statically catches `search_path = public` regressions on migrations ≥0032.
- **ISSUE-012** — 6 FK columns referencing `ports` (`entry_port_id`, `destination_port_id`, etc. on
  `shipments` and `in_bond_records`) have no index anywhere — seq-scan risk on port-filtered queries.
  **Resolution:** Fixed in `b0cc671` — migration 0043 indexes all 6 previously-unindexed port FK columns on `shipments`/`in_bond_records`.

### Domain / Integrations

- **ISSUE-013** — Same "ACE ships CA→US / ACI ships US→CA" business rule implemented independently in
  `registry.ts:343` and `movement-validation.ts:188` — drift risk on a future regime change.
  **Resolution:** Fixed in `5c66926` — `movement-validation.ts` now calls the shared `expectedPartnerCountry` from `registry.ts` instead of duplicating the ACE/ACI direction rule.
- **ISSUE-014** — No Stripe `idempotencyKey` on checkout/portal session creation —
  `packages/integrations/src/stripe.ts:164,186` — a retried call can mint duplicate sessions.
  **Resolution:** Fixed in `185f912` — Stripe checkout/portal session creation now passes an `attemptId`-based `idempotencyKey`, preventing duplicate sessions on retry.
- **ISSUE-015** — `valueAmount` (declared cargo value) modeled as floating point, not integer cents/decimal
  — `packages/domain/src/shipment.ts:146`, `document.ts:45,105`.
  **Resolution:** Fixed in `915632e` — a shared `moneyAmount` zod schema (cents precision, bounded by `numeric(14,2)`) is now applied at all three cargo/shipment value sites, and the AI extraction pipeline rounds extracted values to cents before validation.
- **ISSUE-016** — Same latent "fresh client = lost state" defect as ISSUE-003 exists in the mock client's
  `fetchStatus`, currently unreached but a trap for future callers —
  `packages/integrations/src/customs/mock.ts:81-84,201-212`.
  **Resolution:** Fixed in `aa290d6`/`374752e` — the same tenant-scoped, bounded fixture-state fix applied to the gateway client also covers the mock client's `fetchStatus`, closing the latent trap before any caller hit it.

### AI / PDF

- **ISSUE-017** — No size/token cap on document text sent to the extraction model (unlike the classifier's
  8192-byte cap) — `packages/ai/src/document-intelligence/model-extractor.ts:58`.
  **Resolution:** Fixed in `546f2e2` (typecheck follow-up in `2c8d201`) — a shared `readableText`/`EXTRACTION_TEXT_BYTES` cap now bounds the extractor's input, matching the classifier's existing cap.
- **ISSUE-018** — No timeout/abort signal on `generateObject`/`embed`/`embedMany` calls —
  `model-extractor.ts:69`, `copilot/embedder.ts:39,43`.
  **Resolution:** Fixed in `546f2e2` (typecheck follow-up in `2c8d201`) — a new `aiTimeoutSignal` bounds `generateObject`/`embed`/`embedMany` calls in the extractor and embedder.
- **ISSUE-019** — Retrieved org-authored knowledge is concatenated into the copilot system prompt with only
  a `[K1]` label, no escaping/fencing — weak prompt-injection resistance for content one org member writes
  and another's copilot session reads — `packages/ai/src/copilot/prompts.ts:10-25`.
  **Resolution:** Fixed in `8bc7d0a` — retrieved excerpts are now wrapped in `<excerpt id=... source=...>` tags with injected fence-tags stripped, plus a system-prompt rule that excerpt content is data, not instructions.
- **ISSUE-020** — `packages/pdf` templates have no internal page/row cap of their own (the only guard lives
  in `packages/domain/src/pdf.ts:32` for one report type; `table-report.tsx` rows are unbounded within the
  package itself).
  **Resolution:** Fixed in `26dd2fa` — `MAX_TABLE_REPORT_ROWS`/`truncateTableRows` now cap PDF table rows inside `packages/pdf` itself, independent of caller-side guards.

### Auth / UI / Config

- **ISSUE-021** — `resolveUser`/`toSessionUser` (the single source of truth for turning a Supabase client
  into a `Session`) has no dedicated test file — `packages/auth/src/session.ts:34-70`.
  **Resolution:** Fixed in `f8c573a` — added `packages/auth/src/session.test.ts` covering `resolveUser`/`toSessionUser`; a test-only finding, no production code changed.
- **ISSUE-022** — Theme localStorage key `"corridor-theme"` hardcoded independently in two places with no
  shared constant — `packages/ui/src/components/theme-toggle.tsx:7` vs `apps/web/src/app/layout.tsx:36`. A
  future rename breaks SSR flash-prevention silently.
  **Resolution:** Fixed in `423e07c` — a shared `THEME_STORAGE_KEY` constant in `packages/ui/src/lib/theme.ts` is now used by both `ThemeToggle` and the web app's pre-paint script.
- **ISSUE-023** — `Checkbox`, `RadioGroup`/`RadioGroupItem`, `Switch`, and raw `Table` primitives are
  exported but have zero call sites outside `packages/ui` — maintained-but-unused surface area.
  **Resolution:** Fixed in `44f6f53` — the unused `Checkbox`/`RadioGroup`/`RadioGroupItem`/`Switch`/raw `Table` exports were removed from the UI package's barrel.
- **ISSUE-024** — `@corridor/config` exports `./eslint/next.js` in `package.json` but the file doesn't
  exist on disk — broken package export, currently unused so unnoticed.
  **Resolution:** Fixed in `44f6f53` — the dangling `./eslint/next.js` package export was removed from `@corridor/config`'s `package.json`.

### Web app

- **ISSUE-025** — `PUBLIC_PATHS` lists `/auth/confirm`, but no such route exists anywhere in the app — dead
  allowlist entry — `apps/web/src/proxy.ts:9`.
  **Resolution:** Fixed in `5454b86` — the dead `/auth/confirm` entry was removed from `PUBLIC_PATHS`.

### Mobile

- **ISSUE-026** — Expo push token (classified by the app's own README as a "bearer capability") sits in
  plaintext `AsyncStorage` while queued in the offline outbox, inconsistent with its stated sensitivity —
  `apps/mobile/src/lib/outbox-client.ts:44-49`, `outbox.ts:314-318`, `push.ts:57-60`.
  **Resolution:** Fixed in `092f026` — the Expo push token is now saved via `packages/mobile/src/lib/push-token-store.ts` (SecureStore) and read at send time; the outbox entry carries only `{ platform }`, never touching AsyncStorage.

### Recent changes

- **ISSUE-027** — Trigram indexes added in migration 0034 don't match the actual search predicates:
  `movement.list`'s driver search matches a _concatenated_ `first_name || ' ' || last_name` expression that
  no single-column index can accelerate; `trip_number`/`customs_reference_number` also searched but
  unindexed — `supabase/migrations/0034_search_indexes.sql` vs `packages/api/src/router/movement.ts:150-158`.
  **Resolution:** Fixed in `b0cc671` — migration 0043 adds trigram indexes matching `movement.list`'s actual search predicates (concatenated driver name, `trip_number`, `customs_reference_number`).
- **ISSUE-028** — Customs decision is applied against a stale in-memory movement snapshot across two
  separate transactions with no optimistic-concurrency guard (`UPDATE ... WHERE id = m.id`, no
  `AND status = from`) — a concurrent edit during the external gateway call can be silently clobbered —
  `packages/api/src/services/jobs.ts:197-237`, `packages/api/src/services/movements.ts:260-264`.
  **Resolution:** Fixed in `f836f41` — `lockMovement` performs a row-locking read and `applyTransition`'s update now guards with `and(eq(status, from))`, throwing `CONFLICT` on a stale write; closes the race between a background job's read and its slow external gateway call.

---

## LOW

- **ISSUE-029** — Inconsistent SQL `LIKE`-wildcard escaping: copilot's `checkMovementStatus` tool doesn't
  escape `%`/`_` in `movementNumber`, unlike every other search path including its own sibling function —
  `packages/api/src/services/copilot.ts:233-240`.
  **Resolution:** Fixed in `74fe359` — a shared `escapeLike`/`containsPattern` helper (`packages/api/src/infra/like.ts`) replaces 9 duplicated inline regexes, including the previously-unescaped `lookupMovementStatus` site.
- **ISSUE-030** — Authorization/lookup/audit boilerplate (`permissionProcedure → require row → mutate →
writeAudit`) hand-rolled near-identically across `movement.ts`/`shipment.ts`/`inbond.ts`/`organization.ts`
  instead of factored like `party.ts`'s `registryRouter`.
  **Resolution:** Partially addressed in `7009255` — a shared `mapDbError` helper (`packages/api/src/services/db-errors.ts`) was extracted from `party.ts`/`inbond.ts`'s duplicated Postgres-error mapping. The broader `registryRouter`-style factoring across `movement.ts`/`shipment.ts`/`organization.ts` was explicitly scoped out (see the plan's Task 25 scope note); two other `inbond.ts` catches with custom error messages were deliberately left alone to avoid changing caller-facing text.
- **ISSUE-031** — `sql.raw()` string interpolation in a test file (not exploitable — input is a fixed enum)
  — `packages/db/src/movements.integration.test.ts:131-132`.
  **Resolution:** Fixed in `fcd3441` — the test's `sql.raw` string interpolation was replaced with genuine bound array parameters (`conn.sql` with `${fs}::text[]`).
- **ISSUE-032** — "Why a new table" header convention absent from all 24 tables created before migration
  0018 — the rule as written doesn't grandfather them (including the migration CONTRIBUTING.md cites as the
  model, 0013).
  **Resolution:** Fixed in `679c0a7` — `CONTRIBUTING.md`/`CLAUDE.md` now state the "Why a new table" rule applies from migration 0018 onward, grandfathering the 32 pre-0018 tables, and cite `0028_import_batches.sql` as the reference migration.
- **ISSUE-033** — Silent catches swallowing localStorage/JSON parse failures with no logging —
  `apps/web/src/app/(app)/reports/crossing-report.tsx:42-54`, `use-list-prefs.ts:27-51`,
  `help/manual.ts:24-25`.
  **Resolution:** Fixed in `5454b86` — the silent catches in `crossing-report.tsx`, `use-list-prefs.ts`, and `help/manual.ts` now log a bracket-tagged message on failure, with the same fallback behavior.
- **ISSUE-034** — Cookie-options literal (`httpOnly`/`secure`/`sameSite`) duplicated near-verbatim across 5
  files instead of one shared helper — `proxy.ts:43-46`, `lib/supabase/server.ts:24-27`, and 3 server-action
  files.
  **Resolution:** Fixed in `5454b86` — a shared `appCookieOptions` helper replaces the 5 duplicated cookie-options literals.
- **ISSUE-035** — `reportUsage` aborts the whole batch on the first Stripe failure, discarding
  already-computed results (safe only because Stripe's own idempotency key de-dupes retries) —
  `packages/integrations/src/stripe.ts:234-251`.
  **Resolution:** Fixed in `185f912` — `reportUsage`'s batch loop now catches per-record failures instead of discarding the whole batch on the first error.
- **ISSUE-036** — `normalisePhone` assumes any bare 10-digit number is North American and prepends `+1`,
  silently corrupting non-NA numbers that happen to be 10 digits — `packages/integrations/src/sms.ts:42-48`
  (documented as a deliberate simplification).
  **Resolution:** Fixed in `4c824ce` — `normalisePhone` was rewritten on `libphonenumber-js` with an explicit `defaultCountry` parameter, no longer assuming any 10-digit number is North American.
- **ISSUE-037** — Unused `expo-linking` dependency / registered `corridor://` scheme with no deep-link
  handling code anywhere — dead dependency, unexercised attack surface.
  **Resolution:** Fixed in `fbedea0` — the unused `expo-linking` dependency was removed; `app.json`'s `scheme` was retained since expo-router uses it.
- **ISSUE-038** — `EXPO_PUBLIC_API_URL` unset silently degrades to `""`, producing an opaque relative-fetch
  failure instead of a clear error — `apps/mobile/src/lib/env.ts:7-14`, `trpc.ts:27`.
  **Resolution:** Fixed in `b0acb85` — mobile `env.ts`'s `required()` now throws at import time instead of warning and silently falling back to `""`.
- **ISSUE-039** — Notification cursor pagination (`nextCursor`) added on the API is never consumed by either
  the web or mobile notification list — dead client-side plumbing, not a regression.
  **Resolution:** Fixed in `2883af9` — the web notifications list now uses `useInfiniteQuery`/`infiniteQueryOptions` with a "Load more" button, and the mobile notifications screen pages via a new `appendPage` reducer with `FlatList`'s `onEndReached`.
- **ISSUE-040** — Historic unindexed FK gap on `cargo.shipper_id`/`consignee_id` — moot, columns were
  dropped in migration 0019 when `cargo` was renamed to `commodities`.
  **Resolution:** Closed as moot, no code change — `cargo` was renamed to `commodities` and `shipper_id`/`consignee_id` moved to `shipments` in migration 0019 (predates this branch); no unindexed FK remains, confirmed independently during Task 5's exploration.
- **ISSUE-041** — `packages/config`'s base ESLint config only extends untyped `tseslint.recommended`, not
  `recommendedTypeChecked`/`strict` — type-aware rules like `no-floating-promises` aren't enforced
  monorepo-wide via lint (not a claim violation, just a gap).
  **Resolution:** Partially fixed in `7172d4e`, per a controller ruling recorded in `b3d4623`. Type-aware ESLint (`recommendedTypeChecked`) is now enabled monorepo-wide, and ~122 mechanical sites (`no-unnecessary-type-assertion`, `require-await`, one `no-misused-promises`, ~14 `allowDefaultProject` config fixes) were corrected. 10 rule families needing real type-safety judgment across `any`-typed boundaries (`no-base-to-string`, `no-unsafe-assignment`, `no-unsafe-return`, `no-unsafe-member-access`, `no-unsafe-call`, `no-unsafe-argument`, `restrict-template-expressions`, `no-floating-promises`, `unbound-method`, `no-redundant-type-constituents`) are explicitly disabled by name in `packages/config/eslint/base.js` with a dated comment — a documented, tracked follow-up (229 sites), not silent acceptance.

---

## What couldn't be verified (would need runtime/browser testing)

End-to-end behavior of UI handlers (billing checkout, SSO, imports, etc.) — static tracing found every
sampled handler wired to a real mutation, no dead buttons found in the sample, but this wasn't exercised in
a live browser. Cookie flags as sent over the wire, color-contrast ratios in `packages/ui/src/tokens.css`,
and whether `movement`/`documents` tRPC routers apply the same session-derived scoping verified for
`notifications` were also not independently re-checked (each is a small piece of a package another agent
already covered).
